{
  "name": "goghwiththeflow",
  "private": true,
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "collect:met": "node scripts/collect-met.mjs",
    "download:images": "node scripts/download-images.mjs",
    "compute:features": "node scripts/compute-features.mjs",
    "pipeline:v1": "npm run collect:met && npm run download:images && npm run compute:features"
  },
  "dependencies": {
    "image-js": "^0.35.6",
    "node-vibrant": "^4.0.4",
    "three": "^0.161.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
